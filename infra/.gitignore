# Terraform state — contains resource IDs and sensitive values
*.tfstate
*.tfstate.backup

# Variables with secrets (PAT, unique names)
*.tfvars

# Provider cache (binaries downloaded by terraform init)
.terraform/

# Lock file (regenerated by terraform init)
.terraform.lock.hcl
